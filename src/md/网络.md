# tcp

https://www.ruanyifeng.com/blog/2017/06/tcp-protocol.html
http://www.ruanyifeng.com/blog/2009/03/tcp-ip_model.html

tcp 保证数据通信的完整性和可靠性

以太网-ip-tcp-应用层
tcp 负载大概 1400byte

tcp 包有编号，每个 tcp 包存储自己的编号和下一个包的编号，：防丢失和乱序

收到 TCP 数据包以后，组装还原是操作系统，之后发送给应用程序

TCP 数据包里面有一个端口（port）参数，就是用来指定转交给监听该端口的应用程序。

80 端口是 Web 服务器

一次 TCP 通信可以包括多个 HTTP 通信？ // todo how

默认情况下，接收方每收到两个 TCP 数据包，就要发送一个确认消息。"确认"的英语是 acknowledgement，所以这个确认消息就简称 ACK。

ACK 确认信息，携带期待下一个包的编号，

如果发送方发现收到三个连续的重复 ACK，或者超时了还没有收到任何 ACK，就会确认丢包，即 5 号包遗失了，从而再次发送这个包。通过这种机制，TCP 保证了不会有数据包丢失。

三次握手： 双方都可以确认自己、对方都放松、接受正常。四次没有必要
四次挥手

# http1.1

1. Conection: keep-alive, 避免一次连接后关闭 tcp，下一次重用 tcp
   浏览器限制了同一个域名下的请求数量 ： 雪碧图，小图内联，多域名

请求头数据协商

# http2

1. 压缩了 http 协议头
2. 二进制传输代替文本传输
3. 多路复用： http1.1 通过 connection keep-alive ，可以复用 tcp，但是不能用同一个 tcp 同时发送多个请求。http2 可以通过一个 tcp 连接，同时发送多个 http 请求（同域名）。
4. 服务端推送

# https

1. 443
2. 对称加密，加密解密密钥相同。 des。 非对称加密，gaoyaosiyao： rsa
3. https 通信过程：
   ssl/tls 加密解密，http 传输
4. 涉及对称加密、非对称加密。3 个密钥： 服务器公钥、私钥， 浏览器随机生成的密钥
   中间人攻击\伪造服务器 =》 ca 认证机构\证书的验证

（6.3.1）证书包含什么信息？颁发机构信息+公钥+公司信息+域名+有效期+指纹 : 中间人即使拿到证书也无法伪装成合法服务端，因为无法对客户端传入的加密数据进行解密。

## cdn 原理，算法

# 缓存

1. cahce-control: no-public no-private no-cache no-store no-transform
   到期 max-age= 重新验证 must-revalidate
   expires 旧的 固定的过期时间
2. ETag, 服务器返回 etag，浏览器后续请求头 If-None-Match 带上 etag 与服务器上最新的 etag 比较，一样就返回 304
3. Last-Modified （if-modified-since）类似 Etag， etag 优先级更高

## bfc,最小的布局，所有元素都会在内部，不会影响外部

0. Once an element creates a BFC, everything is contained inside it
1. float
2. overflow not visible
3. position： absolute fixed
4. display: inline-block table-cell
5. display: flow-root

6. flex,grid 会创建 ffc gfc

bfc,清除浮动，防止 margin 折叠。
属于同一个 BFC 的两个相邻 Box 的 margin 会发生重叠

盒模型： box-sizing:
标准盒模型： css width 是 content 的。 border
1px transform

todo: webpack 加载实现, 手写 promise。。  封装table。 useTree. loadingBtn  小程序开发的坑。 webworker-react
webpack 的优化原理跟内存泄露检测么

三剑客基础、框架全家桶使用及原理、前端工程化、计算机网络、浏览器、前端各领域热点话题（错误监控，性能监控，微前端，跨端开发...）
