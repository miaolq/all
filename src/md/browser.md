# 多进程架构

主进程、网络进程、GPU 进程、多个渲染进程、多个插件进程

# 渲染进程下的多线程

主线程、webWorker 线程、compositor(合成)线程、raster(光栅化)线程

1. parse DOM
   1.1 预解析，
   If there are things like <img> or <link> in the HTML document, preload scanner peeks at tokens generated by HTML parser and sends requests to the network thread in the browser process.
   1.2 js下载、执行阻塞parsing （除async defer）
   1.3 提示浏览器如何加载 async defer
2. cssOM + dom => renderTree

## 参考

1. https://developers.google.com/web/updates/2018/09/inside-browser-part3
